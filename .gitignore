.superpowers/
.worktrees/
worktrees/
node_modules/
formspree.json

# Python / GA4 fetch
.venv/
venv/
secrets/*.json
# Meta / Facebook marketing API (page token — niet committen)
secrets/*.env

# GA4 export (lokaal gegenereerd; kan vertrouwelijke aggregaties bevatten)
docs/website-manager/ga4_report.json

# Lighthouse batch output (lokaal / productie JSON)
reports/lighthouse/
reports/lighthouse-production/
reports/lighthouse-production-after/
reports/lighthouse-production-verify/
reports/lighthouse-index-lcp/
