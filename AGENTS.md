## Learned User Preferences

- Before recommending a local dev URL, validate it: `curl` the URL from the same machine, expect `200`, `text/html`, body starting with `<!doctype` (or project `<title>`), and **not** a “Directory listing” page; if the port is already taken by another process, use another port and document it. A directory listing almost always means the server was started from the wrong working directory or the wrong process owns the port.
- Prefer clarifying questions one at a time and include how many questions remain.
- Prefer multiple-choice clarifying questions when possible.
- Require explicit permission before creating new `.md` files.
- Dislikes using worktrees in general; only use them when they clearly help and can be justified briefly.
- For VLWarmte warranty copy, describe pipe (buis) warranty as a straight “10 jaar garantie”; avoid “fabrieksgarantie” and avoid hedging with supplier/type conditions unless the user explicitly asks for that nuance.
- For the contact/offerte form, prefer a vertical field pattern (label, optional hint, then input). Avoid side-by-side label+input rows that use large spaces or spacers to align inputs, which hurt scanability and label–field association.
- For FAQ or accordion blocks, do not set the question title smaller or visually weaker than the answer body; the title should be at least as prominent for clear hierarchy.
- Do not commit Formspree deploy keys or a tracked `formspree.json` that contains secrets; keep those files gitignored and ship only a safe `.example` template in the repo.
- **Proactive assistant:** Default to performing runnable steps immediately (shell, reads, edits, verification) instead of only telling the user what to run; offer or continue with the next actionable step when something still requires the human (secrets in their env, browser login, irreversible confirmations). For “today” / current-year questions in this workspace, treat the session **Today's date** field in user info as authoritative when present (do not assume a default training year).

## Learned Workspace Facts

- Workspace at `/Users/hanseilers/vlwarmte` is used for the VLWarmte website redesign workstream.
- Design specs and implementation plans are maintained under `docs/superpowers/`.
- Current VLWarmte redesign direction favors a modern/tighter, potentially darker look with stronger imagery.
- VLWarmte's preferred logo direction is an SVG "Slakkenhuis Flow" underfloor-heating laying pattern using the site's CTA accent color, without a surrounding border.
- Mobile responsiveness is a priority for the VLWarmte site, especially keeping the header and navigation compact and visible on small screens.
- Contact lead form has three modes (informatie, offerte, bel mij); offerte mode collects crawl-space depth in mm in field `vloerdiepte`, with user-facing copy centered on “Diepte kruipruimte” and a short hint tying the measure to build-up and chape/beton planning (not a generic “floor thickness only” label); “Bel mij” requires `terugbel_moment` (preset callback slots) and submits hidden `soort_aanvraag` (`Informatie` | `Offerte` | `Terugbelverzoek`) for Formspree.
- **Contact deep links (marketing/campagnes):** `contact.html` can open scrolled to the lead block and with the correct tab. Use fragment `#aanvraag` on the section that contains the mode switch + form. Use query **`modus`** or **`tab`** (either name works; values are case-insensitive) to preselect the tab: `info` / `informatie`, `offerte`, `bel` / `terugbel` / `terugbelverzoek`. Recommended pattern: `https://www.vlwarmte.nl/contact.html?modus=offerte#aanvraag`. Scroll-only: `…/contact.html#aanvraag`. `#lead-form` also triggers a scripted scroll to the same block (keeps tabs visible). Implementation: `assets/js/main.js` (`readLeadModeFromUrl`, `leadFormDeepLinkNeedsJsScroll`, `scrollLeadFormAnchorIntoView`).
- VLWarmte mobile header/menu: do not put `backdrop-filter` on the same element that contains a `position: fixed` nav drawer; Chromium can treat it as a containing block and clip the drawer, so apply frosted blur on a `::before`; the open-state `::after` dimmer must sit below the inner wrapper and drawer (raise `.site-header-inner`’s `z-index` when open) so it does not paint over menu links.
- Local GA4 tooling: `scripts/ga4_fetch.py` — maak `.venv` en installeer `scripts/requirements-ga4.txt`, run met `.venv/bin/python scripts/ga4_fetch.py`; service-account JSON op een Terminal-leesbaar pad (e.g. gitignored `secrets/`, `GA4_CREDENTIALS_PATH`, or `GOOGLE_APPLICATION_CREDENTIALS`; avoid iCloud `Mobile Documents` paths); property `534641753`; common custom events in frontend include `contact_submit`, `wizard_lead_submit`, `lead_form_submit`, `wizard_calculate`, `calculator_result`, `wizard_start`, and **`facebook_outbound`** (Facebook link clicks in `assets/js/main.js`; optionally mark as a key event in GA4 admin); `docs/website-manager/ga4_report.json` is gitignored.
- **Google Ads + GA4 (optional, lokaal):** `cp secrets/google-ads.env.example secrets/google-ads.env` then set only `GOOGLE_ADS_DEVELOPER_TOKEN` (API Center). Scripts auto-reuse the same service-account JSON as `scripts/ga4_fetch.py` unless `GOOGLE_ADS_JSON_KEY_FILE_PATH` or OAuth client vars are set. Invite that service account email under Google Ads → Admin → Access and security → Users. `pip install -r scripts/requirements-google-ads.txt`; `python scripts/google_ads_smoke_test.py`; `python scripts/google_ads_print_customer_ids.py` (digits-only ids for `GOOGLE_ADS_CUSTOMER_ID`; not gcloud); read-only `python scripts/google_ads_list_campaigns.py` (`GOOGLE_ADS_CUSTOMER_ID` or `--customer-id`); full lead Search campaign (budget, NL geo, ad group, keywords, RSA from `scripts/data/google_ads_lead_campaign_defaults.json`): `python scripts/google_ads_create_search_campaign.py --dry-run …` then `--apply`; optional `--go-live` enables serving (budget cap, `VLW-API-` prefix). Optional **second RSA** (ad strength / variety): `python scripts/google_ads_add_rsa_variant.py --campaign-id <id> --dry-run` then `--apply` (copy under `extra_rsa` in the same JSON). Post-launch: `scripts/google_ads_campaign_next_steps.py negatives|enable` (see skill). Intended for agent/PM execution without Ads UI knowledge; **agents run verification locally** (`list_campaigns`, smoke test, etc.) and do not delegate routine terminal steps to the product owner (only spend approval, missing secrets on that machine, billing/policy). **Agent playbook:** `.cursor/skills/google-ads-marketing/SKILL.md`. PM references the same skill via `.claude/commands/product-manager.md`. Do not commit keys or refresh tokens.
- Slash-command playbooks for agents live under `.claude/commands/` (e.g. developer, website-manager, analytics, marketing-research, social-media, product-manager); treat them as workflows to follow when invoked in Cursor. There is no separate **product-owner** playbook—use **product-manager** (`.claude/commands/product-manager.md`) for `docs/website-manager/` sprint and backlog orchestration when the user asks for a product owner. After the developer agent finishes, the **product-manager** playbook expects the PM agent to **`git commit` and `git push origin main`** (GitHub Pages) itself—no need to ask the business owner unless git authentication is missing on that machine. Customer imagery for social/website work is stored in committed WhatsApp exports under `beeldmateriaal/`; generated social assets may still live under `docs/website-manager/social/input/`. For **Facebook profile/cover creative** (not keyword research), prefer the **`social-media-agent`** playbook (`.claude/commands/social-media-agent.md`) over **`marketing-research-agent`**.
- **Meta Graph API:** `secrets/meta-facebook.env` (gitignored) stores `META_GRAPH_ACCESS_TOKEN` (system user token — not a page-level token), `META_PAGE_ID` (`1068733929659423`), optional `META_APP_ID` and `META_GRAPH_API_VERSION`; use `.cursor/skills/meta-graph-api/SKILL.md` for correct Graph API usage; for Facebook Page photo publishing, first resolve the Page access token with `GET /{page-id}?fields=access_token` using the system user token, then post to `/{page-id}/photos` with the Page token to avoid `publish_actions`/403 failures.
- **Facebook on vlwarmte.nl:** Public page URL `https://www.facebook.com/1068733929659423` is linked from the **footer** on root HTML pages, from **`contact.html`** (bedrijfsgegevens + footer), and from **`prijsindicatie.html`** “Bedrijf” footer nav. **`index.html`** includes the same URL in `LocalBusiness` JSON-LD `sameAs`. Use the on-site **`.btn-social-facebook`** pill (see `assets/css/styles.css` and mirrored rules in `assets/css/critical-index.css`) instead of embedding Facebook’s Like SDK by default (fewer third-party cookies/weight; real like/follow happens on Facebook after click). **`prijsindicatie.html`** loads `assets/js/main.js` (deferred) so those links and mobile nav match the rest of the site and still emit `facebook_outbound` when `gtag` is present.
- **Pricing wizard (`prijsindicatie.html`) product-first flow:** First question is product choice (alleen vloerverwarming vs. vloerverwarming met schuimbeton); hout-ondergrond routes to a contact flow (no price shown). **Schuimbeton path** shows a **rough € band** (excl. btw): caps schuimbeton at ~400 mm “thermisch zinvol”, treats deeper fill as **zandophoging** (m³ × indicative €/m³), adds a VL component per m² woning, and applies a small multiplier by kruip-ondergrond — still clearly “indicatie”, not a quote. `logo-varianten.html` has been removed from the site.
