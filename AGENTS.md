## Learned User Preferences

- Before recommending a local dev URL, validate it: `curl` the URL from the same machine, expect `200`, `text/html`, body starting with `<!doctype` (or project `<title>`), and **not** a “Directory listing” page; if the port is already taken by another process, use another port and document it. A directory listing almost always means the server was started from the wrong working directory or the wrong process owns the port.
- Prefer clarifying questions one at a time (say how many remain) and use multiple-choice options when possible.
- Require explicit permission before creating new `.md` files.
- Dislikes using worktrees in general; only use them when they clearly help and can be justified briefly.
- Public site copy should not claim strict in-house-only delivery or “geen onderaanneming” when trusted subcontractors are part of execution; avoid unnecessary public detail about how work is staffed unless accurate and intentional.
- For Google Ads campaign creatives, prefer real customer/project photos from `beeldmateriaal/`; avoid AI-generated imagery (light editing like crop/brightness is fine).
- For VLWarmte warranty copy, describe pipe (buis) warranty as a straight “10 jaar garantie”; avoid “fabrieksgarantie” and avoid hedging with supplier/type conditions unless the user explicitly asks for that nuance.
- For the contact/offerte form, prefer a vertical field pattern (label, optional hint, then input). Avoid side-by-side label+input rows that use large spaces or spacers to align inputs, which hurt scanability and label–field association.
- For FAQ or accordion blocks, do not set the question title smaller or visually weaker than the answer body; the title should be at least as prominent for clear hierarchy.
- Do not commit Formspree deploy keys or a tracked `formspree.json` that contains secrets; keep those files gitignored and ship only a safe `.example` template in the repo.
- **Proactive assistant & accessibility:** Default to performing runnable steps immediately (shell, reads, edits, verification) instead of only telling the user what to run; offer or continue with the next actionable step when something still requires the human (secrets in their env, browser login, irreversible confirmations). For “today” / current-year questions in this workspace, treat the session **Today's date** field in user info as authoritative when present (do not assume a default training year). When the user has limited typing capacity, minimize repetitive manual entry—prefer gitignored `secrets/*.env`, short concrete commands, and scripted or agent-run setup.
- **Public copy tone (Noord-Nederland):** Informal and friendly (`je`/`jij`, not `u`/`uw`); plain language, not stiff or corporate. Avoid words like “realiseert”, “gevestigd”, and “werkzaam”; prefer “meestal” over “doorgaans”. Write reaction times as **één werkdag**, not “1 werkdag”.

## Learned Workspace Facts

- Workspace at `/Users/hanseilers/vlwarmte` is used for the VLWarmte website redesign workstream; in-repo design specs and implementation plans are maintained under `docs/superpowers/`. An external **Claude Design handoff** lives at `docs/designer/vl-warmte/` (`README.md` + `project/` with `index.html`, `tokens.css`, `styles.css`, `app.jsx`, `primitives.jsx`, and `sections/section-1…7-*.jsx`) — treat it as an **audit/recommendation document, not drop-in code**: cherry-pick defensible suggestions for `index.html` and skip changes that fight repo conventions (e.g. its `tokens.css` Google-Fonts swap on the homepage hero — keep the existing system-ui stack in `assets/css/critical-index.css`).
- Current VLWarmte redesign direction favors a modern/tighter, potentially darker look with stronger imagery; preferred logo direction is an SVG "Slakkenhuis Flow" underfloor-heating laying pattern using the site's CTA accent color, without a surrounding border. Canonical brand mark on the live site is **`assets/img/logo-mark-krul.svg`** (with the existing `.brand-text-stack` wordmark) — keep this file as-is and **reject external/designer mocks that swap it for generic gradient-square placeholders**, even when the mock looks polished.
- Mobile responsiveness is a priority (compact header and nav on small screens). For the frosted mobile menu: do not put `backdrop-filter` on the same element as a `position: fixed` drawer (Chromium clipping)—blur on a `::before`; open-state `::after` dimmer sits below the inner wrapper and drawer (raise `.site-header-inner` `z-index` when open) so it does not cover links.
- **Mobile ATF vs desktop order:** Many inner pages use `page-hero` (copy-focused) with **primary CTAs in the following `cta-band`**; CSS does not reorder content for small viewports relative to desktop, so **first-screen CTA visibility** depends heavily on viewport height—when editing conversion-sensitive templates, check that **at least one primary action** is plausibly visible without scrolling on typical phones, not only that desktop and mobile share the same DOM order.
- Contact lead form has three modes (informatie, offerte, bel mij); offerte mode collects crawl-space depth in mm in field `vloerdiepte`, with user-facing copy centered on “Diepte kruipruimte” and a short hint tying the measure to build-up and chape/beton planning (not a generic “floor thickness only” label); “Bel mij” requires `terugbel_moment` (preset callback slots) and submits hidden `soort_aanvraag` (`Informatie` | `Offerte` | `Terugbelverzoek`) for Formspree.
- **Contact deep links (marketing/campagnes):** `contact.html` can open scrolled to the lead block and with the correct tab. Use fragment `#aanvraag` on the section that contains the mode switch + form. Use query **`modus`** or **`tab`** (either name works; values are case-insensitive) to preselect the tab: `info` / `informatie`, `offerte`, `bel` / `terugbel` / `terugbelverzoek`. Recommended pattern: `https://www.vlwarmte.nl/contact.html?modus=offerte#aanvraag`. Scroll-only: `…/contact.html#aanvraag`. `#lead-form` also triggers a scripted scroll to the same block (keeps tabs visible). Implementation: `assets/js/main.js` (`readLeadModeFromUrl`, `leadFormDeepLinkNeedsJsScroll`, `scrollLeadFormAnchorIntoView`).
- Local GA4 tooling: `scripts/ga4_fetch.py` — maak `.venv` en installeer `scripts/requirements-ga4.txt`, run met `.venv/bin/python scripts/ga4_fetch.py`; service-account JSON op een Terminal-leesbaar pad (e.g. gitignored `secrets/`, `GA4_CREDENTIALS_PATH`, or `GOOGLE_APPLICATION_CREDENTIALS`; avoid iCloud `Mobile Documents` paths); property `534641753`; common custom events in frontend include `contact_submit`, `wizard_lead_submit`, `lead_form_submit`, `wizard_calculate`, `calculator_result`, `wizard_start`, and **`facebook_outbound`** (Facebook link clicks in `assets/js/main.js`; optionally mark as a key event in GA4 admin); `docs/website-manager/ga4_report.json` is gitignored.
- **Google Ads + GA4 (optional, lokaal):** `cp secrets/google-ads.env.example secrets/google-ads.env` then set only `GOOGLE_ADS_DEVELOPER_TOKEN` (API Center). Scripts auto-reuse the same service-account JSON as `scripts/ga4_fetch.py` unless `GOOGLE_ADS_JSON_KEY_FILE_PATH` or OAuth client vars are set. Invite that service account email under Google Ads → Admin → Access and security → Users. `pip install -r scripts/requirements-google-ads.txt`; `python scripts/google_ads_smoke_test.py`; `python scripts/google_ads_print_customer_ids.py` (digits-only ids for `GOOGLE_ADS_CUSTOMER_ID`; not gcloud); read-only `python scripts/google_ads_list_campaigns.py` (`GOOGLE_ADS_CUSTOMER_ID` or `--customer-id`); full lead Search campaign (budget, NL geo, ad group, keywords, RSA from `scripts/data/google_ads_lead_campaign_defaults.json`): `python scripts/google_ads_create_search_campaign.py --dry-run …` then `--apply`; optional `--go-live` enables serving (budget cap, `VLW-API-` prefix). Optional **second RSA** (ad strength / variety): `python scripts/google_ads_add_rsa_variant.py --campaign-id <id> --dry-run` then `--apply` (copy under `extra_rsa` in the same JSON). Post-launch: `scripts/google_ads_campaign_next_steps.py negatives|enable` (see skill). Image assets (e.g. `MARKETING_IMAGE`) **cannot be attached to a `SEARCH` campaign** — the API rejects this with `FIELD_TYPE_INCOMPATIBLE_WITH_CAMPAIGN_TYPE`; image-led creatives require a PMax/asset-led campaign type with its own asset group. The customer-list endpoint may return more IDs than the developer token is approved for; non-approved IDs return `DEVELOPER_TOKEN_NOT_APPROVED` on read/write until Basic/Standard access is granted via Ads → Tools → API Center. Intended for agent/PM execution without Ads UI knowledge; **agents run verification locally** (`list_campaigns`, smoke test, etc.) and do not delegate routine terminal steps to the product owner (only spend approval, missing secrets on that machine, billing/policy). **Agent playbook:** `.cursor/skills/google-ads-marketing/SKILL.md` (now also enforces a creative policy: real photos from `beeldmateriaal/`, no AI-generated imagery, no fabricated review/badge metrics). PM references the same skill via `.claude/commands/product-manager.md`. Do not commit keys or refresh tokens.
- Slash-command playbooks for agents live under `.claude/commands/` (e.g. developer, website-manager, analytics, marketing-research, social-media, product-manager); treat them as workflows to follow when invoked in Cursor. There is no separate **product-owner** playbook—use **product-manager** (`.claude/commands/product-manager.md`) for `docs/website-manager/` sprint and backlog orchestration when the user asks for a product owner. After the developer agent finishes, the **product-manager** playbook expects the PM agent to **`git commit` and `git push origin main`** (GitHub Pages) itself—no need to ask the business owner unless git authentication is missing on that machine. Customer/project imagery for social, referrals, and website work: WhatsApp exports at `beeldmateriaal/` root and per-project folders under `beeldmateriaal/projecten/<folder>/` (photos plus optional `README.md` caption); generated social assets may still live under `docs/website-manager/social/input/`. For **Facebook profile/cover creative** (not keyword research), prefer the **`social-media-agent`** playbook (`.claude/commands/social-media-agent.md`) over **`marketing-research-agent`**.
- **Meta Graph API + Facebook on-site:** `secrets/meta-facebook.env` (gitignored) holds `META_GRAPH_ACCESS_TOKEN` (system user — not page token), `META_PAGE_ID` (`1068733929659423`), optional `META_APP_ID` / `META_GRAPH_API_VERSION`; use `.cursor/skills/meta-graph-api/SKILL.md` (Page token via `GET /{page-id}?fields=access_token`, then `POST /{page-id}/photos` with that token). The “Gepubliceerd door X” attribution that Facebook shows on API-published posts is set by the **Meta app / system-user identity behind the access token** (visible via `GET /me?fields=id,name`), **not** by post copy or any API parameter — to brand it (e.g. `VLWarmte` instead of `claude`), rename the Meta app or switch to a different app/system user; otherwise post via the Facebook UI. Public page `https://www.facebook.com/1068733929659423` is in footers (`contact.html`, `prijsindicatie.html` “Bedrijf”), root pages, and `index.html` `LocalBusiness` `sameAs`; use **`.btn-social-facebook`** (`assets/css/styles.css`, `critical-index.css`) instead of the Like SDK. **`prijsindicatie.html`** loads `assets/js/main.js` (deferred) for nav and `facebook_outbound` when `gtag` is present.
- **Hostnet mailbox info@vlwarmte.nl:** IMAP/SMTP automation (not Gmail API) lives in `scripts/hostnet_imap_read.py`; triage/auto-reply workflow uses `scripts/data/hostnet_mail_triage.json` and `scripts/inbox_auto_thankyou.py` to keep `INBOX` catalogued and send customer confirmations when a real lead is detected. Gitignored credentials live in `secrets/hostnet-mail.env` (see `.example`). Agent workflow: `.cursor/skills/hostnet-mail/SKILL.md`.
- **Pricing wizard (`prijsindicatie.html`) product-first flow:** Wizard/calculator sits **above** long explanatory price copy (no bulky intro block above it). First question is product choice (alleen vloerverwarming vs. vloerverwarming met schuimbeton); hout-ondergrond routes to a contact flow (no price shown). **Schuimbeton path** shows a **rough € band** (excl. btw) **without contact details** — say that near CTAs where relevant. Caps schuimbeton at ~400 mm “thermisch zinvol”, treats deeper fill as **zandophoging** (m³ × indicative €/m³), adds a VL component per m² woning, and applies a small multiplier by kruip-ondergrond — still clearly “indicatie”, not a quote. `logo-varianten.html` is retained only as a `noindex` redirect stub to `/` for residual traffic.
